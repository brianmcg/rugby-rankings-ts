const API = 'https://api.wr-rims-prod.pulselive.com/rugby/v3';

export const RANKINGS = `${API}/rankings`;

export const FIXTURES = `${API}/match`;

export const EVENTS = `${API}/event`; // ?sport=mru&pageSize=100`;

export const TEAMS = `${API}/team`; // ?pageSize=100&sports=mru&type=Country&page=0`;

export const SERIES = `${API}/series`; // ?pageSize=100&sport=wrs&page=0`;

export const VENUES = `${API}/venue`; // ?pageSize=100&page=0`;

export const COUNTRIES = `${API}/country`;
